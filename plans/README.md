# Implementation Plans

Generated by the `/improve quick` advisor on 2026-06-17 at commit `f93b72c`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-cors-edge-function.md) | Restrict CORS on AI Edge Function to known origins | P1 | S | — | DONE |
| [002](002-clear-existing-session-bug.md) | Remove destructive session signout from login page | P1 | S | — | DONE |
| [003](003-add-vitest-baseline.md) | Establish Vitest test baseline with coverage of critical paths | P1 | M | — | DONE |
| [004](004-remove-debug-console-logs.md) | Remove debug console.log calls from production hot paths | P2 | S | — | DONE |
| [005](005-npm-audit-fix.md) | Resolve high-severity npm vulnerabilities | P2 | S–M | 003 | DONE |
| [006](006-remove-tempo-devtools.md) | Remove dead tempo-devtools dependency | P2 | S | 003 | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

## Dependency notes

- **005 depends on 003**: npm dependency upgrades are easier to verify safely
  when a build + test baseline exists. If 003 is not yet done, 005 can still
  proceed but the executor must verify manually via `npm run build` only.
- **006 depends on 003**: same reason — removing a dependency is safer with a
  build verification gate.
- **001 and 002 are independent** — execute in any order or in parallel.
- **004 is independent** — can be done at any time.

## Findings considered and rejected

None — all six audit findings were accepted for planning.

## Residual issues (documented, not planned)

- **Plan 005 residual — vite high severity**: `vite ≤ 6.4.2` depends on a vulnerable `esbuild`. Fix requires upgrading Vite 5→6 (major version with breaking config changes). Deferred; this vulnerability is dev-server-only and does not affect the production build. Revisit when Vite 6 migration is ready.
- **Plan 005 residual — uuid/exceljs moderate**: `exceljs ≥ 3.5.0` bundles a vulnerable `uuid < 11.1.1`. Fix would require downgrading exceljs (breaking change). Accepted risk.

## Direction findings (not planned, owner decision)

1. **`CLAUDE.md` created** — Done 2026-06-17.

2. **Remove the redundant Netlify AI function** — `netlify/functions/generate-content.ts`
   still exists but `src/lib/services/aiService.ts` now routes all AI calls
   through the Supabase Edge Function. The Netlify function is dead code that
   will mislead future maintainers. Worth removing once the team confirms the
   Edge Function path is stable. Prerequisite: confirm no path still calls the
   Netlify function.
